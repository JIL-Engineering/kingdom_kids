# Files and directories created by pub
.dart_tool/
.packages

# Pubspec overrides
pubspec_overrides.yaml

# Agents
.dart_skills/
.agents/
.claude/
.gemini/
.cursor/
.vscode/mcp.json
.codex/
.mcp.json

# Serverpod Cloud CLI
.scloud/
.env
**/.env

# Embedded PostgreSQL runtime artifacts (data dir, sockets, pidfiles)
.serverpod/

# IDE workspace config (machine-local paths, personal run configs)
.idea/

# Flutter DevTools per-project preferences
devtools_options.yaml

# AI coding assistant config — local only, not for the shared repo
AGENTS.md
CLAUDE.md
