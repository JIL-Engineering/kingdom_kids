class: PageContent
table: page_content
fields:
  pageId: int, relation(parent=page, onDelete=Cascade)
  language: String
  text: String
  audioAsset: String
indexes:
  idx_page_contents_page_lang:
    fields: pageId, language
    unique: true