class: Book
table: book
fields:
  slug: String, unique
  ageBracketMin: String
  ageBracketMax: String
  category: String
  coverImageAsset: String?
  isPublished: bool
  contentVersion: int
  updatedAt: DateTime
  createdAt: DateTime
indexes:
  books_slug_idx:
    fields: slug
    unique: true