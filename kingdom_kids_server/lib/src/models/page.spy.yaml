class: Page
table: page
fields:
  bookId: int, relation(parent=book, onDelete=Cascade)
  pageNumber: int
  illustrationAsset: String
  layoutType: String
indexes:
  idx_pages_book_number:
    fields: bookId, pageNumber
    unique: true
  idx_pages_book:
    fields: bookId