class: BookTranslation
table: book_translation
fields:
  bookId: int, relation(parent=book, onDelete=Cascade)
  language: String
  title: String
indexes:
  idx_book_translations_book_lang:
    fields: bookId, language
    unique: true