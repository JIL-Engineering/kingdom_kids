class: ChildBadge
table: child_badge
fields:
  childId: int, relation(parent=child_profiles, onDelete=Cascade)
  badgeId: int, relation(parent=badge)
  earnedAt: DateTime
indexes:
  idx_child_badges_child_badge:
    fields: childId, badgeId
    unique: true