class: Badge
table: badge
fields:
  code: String, unique
  name: String
  description: String
  iconAsset: String
  triggerRule: Map<String, dynamic>
indexes:
  badges_code_idx:
    fields: code
    unique: true