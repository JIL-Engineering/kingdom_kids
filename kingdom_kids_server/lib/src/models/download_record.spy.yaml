class: DownloadRecord
table: download_record
fields:
  childId: int, relation(parent=child_profiles, onDelete=Cascade)
  bookId: int, relation(parent=book)
  deviceId: String
  downloadedAt: DateTime
indexes:
  idx_downloads_child_book:
    fields: childId, bookId