class: User
table: user
fields:
  email: String, unique
  passwordHash: String
  country: String?
  timezone: String
  preferredLanguage: String
  consentGivenAt: DateTime?
  createdAt: DateTime
indexes:
  users_email_idx:
    fields: email
    unique: true