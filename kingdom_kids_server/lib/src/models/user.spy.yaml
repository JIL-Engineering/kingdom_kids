class: User
table: users
fields:
  email: String
  passwordHash: String
  country: String?
  timezone: String
  preferredLanguage: String
  consentGivenAt: DateTime?
  createdAt: DateTime
indexes:
  users_email_idx:
    fields: email
    unique: true